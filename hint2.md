### Hint for Positive Negative:
As you've probably noted, this border looks fairly similar to the first one, with the exception that it's `green` instead of red. 

We apply the border around both words separately due to the fact that this class is applied to each separately in a `<span>` element.