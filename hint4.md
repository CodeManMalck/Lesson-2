### Hint for Battery:
There are different types of border styles besides `solid`. Google other border values to see which one you need for this look.

We also need to create some additional space between the border and the text content. This space is called `padding`.